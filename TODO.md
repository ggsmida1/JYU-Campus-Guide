# 嘉应校园导航 —— 二次开发待办清单

---

## 一、图片素材替换

### 1. 其他可复用/需检查的图
`data/media.js` 中以下图片大多可复用（箭头、按钮等UI图标），但要检查：
- `/images/button/` — 功能按钮图标
- `/images/tabbar/` — 底部导航栏图标
- `/images/arrow/` — 翻页箭头
- `/images/map.png` — 使用说明页地图截图（可能需要重新截嘉应学院的地图）

---

## 二、代码数据修改

### 2. `package-lock.json` — 项目名称
第2行 `"name": "桂院校园导航"` 改为 `"嘉应校园导航"`
（注意：这是自动生成文件，`npm install` 时会从 `package.json` 重新生成，所以 `package.json` 也需确认）

---

## 三、云开发后台

### 3. 云数据库
在微信开发者工具「云开发 → 数据库」中创建/导入以下集合：
| 集合名 | 用途 |
|---|---|
| `campuses` | 嘉应学院各校区（江北、江南、梅州师范、医学院） |
| `sites` | 各校区内的地点（教学楼、宿舍、食堂等） |
| `categories` | 地点分类（教学、生活、运动等） |
| `schoolguides` | 校园指南文章 |
| `admins` | 管理员列表 |
| `media` | 轮播图、视频等媒体配置 |

### 4. 云存储
上传替换后的图片到云存储，并更新 `data/media.js` 和 `data/school.js` 中的图片链接。

### 5. 小程序正式发布后重新下载小程序码
当前小程序码为体验版（6月4日前有效），正式发布后需要去微信公众平台重新下载永久有效的小程序码替换。

---

## 四、已完成

| 项目 | 状态 |
|---|---|
| 学校基本信息（校名、校训、简介等）— data/school.js | 已完成 |
| 地图定位坐标 → 嘉应学院 — data/map.js | 已完成 |
| 小程序名称 & 成员信息 — data/data.js | 已完成 |
| .gitignore 完善 | 已完成 |
| 云环境ID / AppID | 已确认（无需修改） |
| 校徽 & 学校图标 | 已完成 |
| API Key（和风天气 + 腾讯地图） | 已完成 |
| 云函数部署（16个） | 已完成 |
| 公众号二维码（4张）— 学校、图书馆、财务处、招生 | 已完成 |
| 联系作者二维码 | 已完成 |
| 小程序码（体验版） | 已完成 |

---

## 五、建议执行顺序

1. 搭建 **云数据库**（第3项），录入校区和地点数据 ← **核心任务**
2. 检查上传 **云存储**（第4项）
3. 修改 `package-lock.json`（第2项）
4. 全流程测试，查漏补缺
5. 正式发布后重新下载小程序码（第5项）
